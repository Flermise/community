<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flermise.community.mapper.QuestionExtMapper">
    <resultMap id="BaseResultMap" type="com.flermise.community.model.Question">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Tue Feb 25 01:36:09 CST 2020.
        -->
        <id column="ID" jdbcType="INTEGER" property="id"/>
        <result column="TITLE" jdbcType="VARCHAR" property="title"/>
        <result column="GMT_CREATE" jdbcType="BIGINT" property="gmtCreate"/>
        <result column="GMT_MODIFIED" jdbcType="BIGINT" property="gmtModified"/>
        <result column="CREATOR" jdbcType="INTEGER" property="creator"/>
        <result column="COMMENT_COUNT" jdbcType="INTEGER" property="commentCount"/>
        <result column="VIEW_COUNT" jdbcType="INTEGER" property="viewCount"/>
        <result column="FAV_COUNT" jdbcType="INTEGER" property="favCount"/>
        <result column="TAG" jdbcType="VARCHAR" property="tag"/>
    </resultMap>
    <update id="incView" parameterType="com.flermise.community.model.Question">
        update QUESTION
        set
        VIEW_COUNT = VIEW_Count + #{viewCount,jdbcType=INTEGER}
        WHERE id = #{id}
    </update>
    <update id="incComment" parameterType="com.flermise.community.model.Question">
        update QUESTION
        set
        COMMENT_COUNT = COMMENT_COUNT + #{commentCount,jdbcType=INTEGER}
        WHERE id = #{id}
   </update>
    <select id="selectRelated" parameterType="com.flermise.community.model.Question" resultMap="BaseResultMap">
        select * from QUESTION where ID !=#{id} and  tag regexp #{tag}
    </select>
    <select id="countBySearch" parameterType="com.flermise.community.dto.QuestionQueryDTO"
            resultType="java.lang.Integer">
        select count(*) from QUESTION
        <where>
            <if test="search != null">
             TITLE regexp #{search}
            </if>
        </where>
    </select>

    <select id="selectBySearch" parameterType="com.flermise.community.dto.QuestionQueryDTO"
            resultMap="BaseResultMap">
        select * from QUESTION
        <where>
            <if test="search != null and search != ''">
                 TITLE regexp #{search}
            </if>
        </where>
        order by gmt_create desc
        limit #{page},#{size}

    </select>
</mapper>